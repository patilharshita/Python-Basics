#Set is an Unordered list of unique values
Meaning, It is a special list where it can not have duplicate values.

Initialization 
Set_Var = {1,3,4,5,6}
List_var = [1,3,4,4,5,6]

#Convert list to set 
set_list = set(List_var) #{1,3,4,5,6}

#Set Comprehension
sample = {x**2 for x in [0,2,4] if x < 4} #{0,4}
